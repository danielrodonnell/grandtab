% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/get_column_metadata.R
\name{get_column_metadata}
\alias{get_column_metadata}
\title{Retrieve column metadata for get_escapement() output}
\usage{
get_column_metadata(
  x = NULL,
  run = NULL,
  river_system = NULL,
  location = NULL,
  summary = NULL,
  section = NULL
)
}
\arguments{
\item{x}{A tibble or named list of tibbles from \code{get_escapement()},
when piped. Leave \code{NULL} (default) for standalone use.}

\item{run}{Character. Salmon run: \code{"lf"}, \code{"w"}, \code{"s"},
\code{"f"}, \code{"all"}, or \code{NULL}. Passed to
\code{get_escapement()}.}

\item{river_system}{Character. River system filter. Passed to
\code{get_escapement()}.}

\item{location}{Character. Location name(s). Passed to
\code{get_escapement()}.}

\item{summary}{Logical. Summary-level data. Passed to
\code{get_escapement()}.}

\item{section}{Integer 1--4. Fall run section. Passed to
\code{get_escapement()}.}
}
\value{
A tibble with columns \code{column_name}, \code{column_position},
  \code{grandtab_raw_name}, and \code{description}. Returns a named list of
  such tibbles when the underlying query returns multiple tables.
}
\description{
Returns a tibble of column metadata describing the columns that
\code{\link{get_escapement}} would return for the same arguments. Can be
used standalone or piped from \code{get_escapement()}.
}
\examples{
# Standalone
get_column_metadata(run = "lf")
get_column_metadata(run = "s", location = "battle")
get_column_metadata(run = "f", section = 1)
get_column_metadata(summary = TRUE)

# Piped from get_escapement()
# get_escapement(run = "w") |> get_column_metadata()
# get_escapement(run = "f", section = 2) |> get_column_metadata()
}
